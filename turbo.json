{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["./dist/**"]
    },
    "build-example": {
      "outputs": ["./dist/**"]
    },
    "test": {
      "outputs": ["./dist/**"]
    },
    "lint": {
      "outputs": []
    }
  }
}
