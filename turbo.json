{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["./dist/**"]
    },
    "test": {
      "outputs": ["./dist/**"]
    },
    "lint": {
      "outputs": []
    }
  }
}
